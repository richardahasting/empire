<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.hastingtx.empire</groupId>
    <artifactId>empire-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>
  <artifactId>empire-server</artifactId>
  <name>empire-server</name>

  <properties>
    <spring-boot.version>4.0.6</spring-boot.version>
    <flyway.version>11.14.1</flyway.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency><groupId>org.hastingtx.empire</groupId><artifactId>empire-engine</artifactId></dependency>
    <dependency><groupId>org.hastingtx.empire</groupId><artifactId>empire-config</artifactId></dependency>
    <dependency><groupId>org.hastingtx.empire</groupId><artifactId>empire-agent-api</artifactId></dependency>
    <dependency><groupId>org.hastingtx.empire</groupId><artifactId>empire-agents</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-web</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-jdbc</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-mail</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-flyway</artifactId></dependency>
    <dependency><groupId>org.flywaydb</groupId><artifactId>flyway-core</artifactId><version>${flyway.version}</version></dependency>
    <dependency><groupId>org.flywaydb</groupId><artifactId>flyway-database-postgresql</artifactId><version>${flyway.version}</version></dependency>
    <dependency><groupId>org.postgresql</groupId><artifactId>postgresql</artifactId><scope>runtime</scope></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-test</artifactId><scope>test</scope></dependency>
    <dependency><groupId>org.hastingtx.empire</groupId><artifactId>empire-sim</artifactId><version>${project.version}</version><scope>test</scope></dependency>
  </dependencies>

  <build>
    <finalName>empire-server</finalName>
    <resources>
      <resource><directory>src/main/resources</directory></resource>
      <!-- the player guide: commands.md is the one source of command syntax, read at runtime by Console -->
      <resource><directory>../docs/guide</directory><targetPath>help</targetPath></resource>
      <!-- the built SPA (run `npm run build` in empire-web first; `make build` does) -->
      <resource><directory>../empire-web/dist</directory><targetPath>static</targetPath></resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!-- every test runs under the "test" profile, which repoints the datasource at empire_test.
               Without this a @SpringBootTest picks up whatever database the environment names, and the
               environment that makes the DB tests run at all is the production one (issue #115). -->
          <systemPropertyVariables>
            <spring.profiles.active>test</spring.profiles.active>
          </systemPropertyVariables>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring-boot.version}</version>
        <executions><execution><goals><goal>repackage</goal></goals></execution></executions>
      </plugin>
    </plugins>
  </build>
</project>
